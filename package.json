{
  "name": "pdf-grabber",
  "version": "1.0.0",
  "description": "Extensión Chromium (Manifest V3) para descargar documentos PDF que el navegador ya recibió y puede visualizar legítimamente.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "icons": "node scripts/generate-icons.mjs",
    "clean": "node scripts/clean.mjs",
    "build:main": "vite build",
    "build:content": "vite build --mode content",
    "build:interceptor": "vite build --mode interceptor",
    "build": "npm run clean && npm run icons && npm run build:main && npm run build:content && npm run build:interceptor && node scripts/postbuild.mjs",
    "dev": "npm run icons && node scripts/dev.mjs",
    "typecheck": "tsc --noEmit",
    "package": "npm run build && node scripts/package.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "typescript": "^5.5.4",
    "vite": "^5.4.8"
  }
}
